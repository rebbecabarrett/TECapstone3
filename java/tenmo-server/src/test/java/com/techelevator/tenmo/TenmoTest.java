package com.techelevator.tenmo;

import static org.junit.Assert.assertNotNull;

import org.junit.Test;

public class TenmoTest extends DAOIntegrationTest {

    // This is just a dummy class to show you how to create an Integration test that extends DAOIntegrationTest
	
	// Remember:  When you create any new test classes, you will need to open the Run Configurations for that class
	// and change JUNIT from JUNIT 5 to JUNIT 4. 
	
	
	
	// This method is just a placeholder to test JUnit.
	@Test
	public void testNotNull() {
		assertNotNull(null);
	}
	
	
}
